export const signUp = async (req, res) => {
  res.json({ msg: "This is a sign up page" });
};

export const signIn = async (req, res) => {
  res.json({ msg: "This is a sign in page" });
};

export const logout = async (req, res) => {
  res.json({ msg: "This is a logout page" });
};
